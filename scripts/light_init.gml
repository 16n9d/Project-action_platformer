/// light_init(rad, col, alpha);
//make the radius, color and alpha variables.
radius=argument0;
color=argument1;
alpha=argument2;

//creating the light surface
lightsurface = surface_create(radius*2,radius*2);
